# 文件夹与文档隐藏显示 Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 为 `md-reader` 增加文件夹与文档隐藏能力、右上角“显示隐藏项”临时切换按钮，以及隐藏项可见模式下的解除隐藏交互，同时保证展开状态、搜索、标签页和 profile 恢复链路稳定。

**Architecture:** 本轮实现分成四层：`WorkspaceProfile.navigation.hiddenPaths` 保存长期隐藏规则，`showHiddenItems` 只保存在 `App` 的会话内 React state 并在刷新后回到 `false`，`src/workspace/file-tree.ts` 基于原始树派生可见性元数据和渲染树，`TopBar` 与 `WorkspaceLayout` 分别承载全局切换和节点级隐藏/解除隐藏操作。`WorkspaceLayout` 不再直接依赖原始树，而是接收 `VisibleFileTreeNode[]` 作为渲染输入，并额外接收 `availableDirectoryPaths` 作为展开状态恢复依据，避免隐藏过滤污染目录展开状态恢复链路。

**Tech Stack:** React 19, TypeScript, Vitest, Testing Library, plain CSS in `src/index.css`

---

## 方案概述

### 1. 总体目标和范围

本计划用于把 [2026-06-20-md-reader-文件夹与文档隐藏显示方案.md](C:/Code/md-reader/docs/方案/2026-06-20-md-reader-文件夹与文档隐藏显示方案.md) 拆解成可执行实施步骤。目标是在不引入额外管理面板、不重做现有文件树结构的前提下，完成：

- 节点级隐藏：文件夹隐藏整个子树，文档隐藏自身
- 全局临时切换：显示/隐藏已隐藏项
- 隐藏项可见状态下的“显示 / 解除隐藏”按钮语义
- profile 级 `hiddenPaths` 持久化
- local-state 级 `showHiddenItems` 临时状态
- 搜索、标签页、当前文档与目录展开状态联动

本计划不包含：

- 批量隐藏/批量恢复
- 独立隐藏项管理页
- 自动规则隐藏
- 移动端专项交互优化

### 2. 各阶段任务概要

阶段一：状态模型扩展

- 主要工作：扩展 `profile-store` 类型，并在 `App` 中落地 `showHiddenItems` 会话态
- 预期成果：`hiddenPaths` 和 `showHiddenItems` 都有明确归属、默认值和测试
- 执行顺序：先状态，再视图

阶段二：原始树与渲染树分离

- 主要工作：在 `file-tree` 层增加隐藏可见性判断与渲染树派生
- 预期成果：隐藏目录不会冲掉已保存的展开状态，搜索和渲染都可复用元数据
- 执行顺序：先纯函数和测试，再接 App 状态

阶段三：顶栏与文件树交互接入

- 主要工作：TopBar 增加“显示隐藏项”按钮，WorkspaceLayout 增加 hover 操作位和弱化样式
- 预期成果：常规/分栏模式都能切换显示隐藏项，并可对节点执行隐藏/解除隐藏
- 执行顺序：先顶栏按钮，再文件树节点交互

阶段四：联动与验证

- 主要工作：补齐搜索、标签页、刷新恢复和目录展开状态回归
- 预期成果：隐藏功能在现有工作区主路径上行为一致
- 执行顺序：功能接通后统一补测试和验证

### 3. 整体结构框架

- `src/workspace/profile-store.ts`
  - profile 级长期导航偏好，新增 `hiddenPaths`
- `src/workspace/file-tree.ts`
  - 原始树构建、隐藏可见性判断、渲染树派生
- `src/App.tsx`
  - profile 读写、`showHiddenItems` 会话态、原始树/渲染树装配
- `src/app/TopBar.tsx`
  - 全局“显示隐藏项”按钮
- `src/app/WorkspaceLayout.tsx`
  - 节点 hover 操作、按钮语义切换、隐藏节点弱化样式与展开状态恢复

## 文件结构与职责映射

- Modify: `src/workspace/profile-store.ts`
  - 为 `WorkspaceProfile.navigation` 增加 `hiddenPaths`
- Modify: `src/workspace/file-tree-types.ts`
  - 增加渲染所需的隐藏元数据类型
- Modify: `src/workspace/file-tree.ts`
  - 增加 `isPathExplicitlyHidden`、`isPathHiddenByAncestor`、`createVisibleFileTree` 与可用目录集合工具
- Modify: `src/App.tsx`
  - 读取/保存 `hiddenPaths` 与 `showHiddenItems`，保留原始树并派生 `visibleFileTree` / `availableDirectoryPaths`
- Modify: `src/app/AppShell.tsx`
  - 透传 `VisibleFileTreeNode[]`、`availableDirectoryPaths` 与隐藏相关 props
- Modify: `src/app/TopBar.tsx`
  - 新增全局“显示隐藏项”按钮，保证 `split` 模式常驻
- Modify: `src/app/WorkspaceLayout.tsx`
  - 新增节点操作区、隐藏/解除隐藏行为、弱化样式和按钮可达性
- Modify: `src/index.css`
  - 新增按钮与隐藏节点样式
- Modify: `tests/workspace/profile-store.test.ts`
  - 锁定 `hiddenPaths` 默认值与持久化
- Modify: `tests/workspace/file-tree.test.ts`
  - 锁定原始树/渲染树分离和隐藏判断
- Modify: `tests/app/workspace-layout.test.tsx`
  - 锁定节点级隐藏、解除隐藏和搜索联动
- Modify: `tests/app/app-shell.test.tsx`
  - 锁定 props 透传
- Optional modify: `tests/app/app-session-restore.test.tsx`
  - 如需锁定刷新后默认回到隐藏视图，则在 App 初始化链路验证
- Create or Modify: `tests/app/topbar-hidden-items.test.tsx`
  - 锁定顶栏按钮、`split` 模式常驻和按钮语义

---

### Task 1: 扩展 profile 状态模型，并明确 `showHiddenItems` 只存在于会话内状态

**Files:**
- Modify: `src/workspace/profile-store.ts`
- Modify: `tests/workspace/profile-store.test.ts`
- Modify: `src/App.tsx`
- Optional modify: `tests/app/app-hidden-items.test.tsx`

- [ ] **Step 1: 写 `profile-store` 失败测试，锁定 `hiddenPaths` 默认值和持久化**

```ts
it('initializes hiddenPaths in the default profile navigation state', async () => {
  const storage = createMemoryKeyValueStore()
  const store = createProfileStore(storage)

  const profile = await store.getProfile('notes', 'default')

  expect(profile.navigation.hiddenPaths).toEqual([])
})

it('persists hiddenPaths alongside other navigation preferences', async () => {
  const storage = createMemoryKeyValueStore()
  const store = createProfileStore(storage)

  await store.saveProfile('notes', {
    ...createDefaultProfile('default'),
    navigation: {
      expandedFileNodes: ['docs'],
      expandedHeadingNodes: {},
      hiddenPaths: ['docs/private', 'docs/secret.md'],
    },
  })

  const restored = await store.getProfile('notes', 'default')
  expect(restored.navigation.hiddenPaths).toEqual(['docs/private', 'docs/secret.md'])
})
```

- [ ] **Step 2: 写 App 初始化失败测试，锁定 `showHiddenItems` 刷新后默认是 `false`**

```tsx
it('starts with showHiddenItems disabled on a fresh mount', async () => {
  render(<App />)

  await waitFor(() => {
    expect(screen.getByRole('button', { name: '显示隐藏项' })).toHaveAttribute('aria-pressed', 'false')
  })
})
```

- [ ] **Step 3: 运行状态层测试，确认失败**

Run: `npm test -- tests/workspace/profile-store.test.ts tests/app/app-hidden-items.test.tsx`  
Expected: FAIL，提示 `hiddenPaths` 字段不存在，或 `showHiddenItems` 相关按钮尚未接入。

- [ ] **Step 4: 扩展 `WorkspaceProfile` 和默认 profile**

```ts
export interface WorkspaceProfile {
  id: string
  appearance: {
    theme: ThemeMode
    fontSize: number
    pageWidth: PageWidthMode
    lineHeight?: DocumentLineHeight
  }
  layout: {
    sidebarWidth: number
    outlineWidth: number
    sidebarCollapsed: boolean
    outlineCollapsed: boolean
  }
  navigation: {
    expandedFileNodes: string[]
    expandedFileNodesInitialized?: boolean
    expandedHeadingNodes: Record<string, string[]>
    hiddenPaths: string[]
  }
}
```

```ts
navigation: {
  expandedFileNodes: [],
  expandedFileNodesInitialized: false,
  expandedHeadingNodes: {},
  hiddenPaths: [],
}
```

- [ ] **Step 5: 在 `App` 中增加会话内 `showHiddenItems` 状态，且不写入任何 store**

```ts
const [showHiddenItems, setShowHiddenItems] = useState(false)
```

```ts
useEffect(() => {
  setShowHiddenItems(false)
}, [activeProjectId, activeProfileId])
```

- [ ] **Step 6: 运行状态层测试，确认通过**

Run: `npm test -- tests/workspace/profile-store.test.ts tests/app/app-hidden-items.test.tsx`  
Expected: PASS

- [ ] **Step 7: Commit**

```bash
git add src/workspace/profile-store.ts src/App.tsx tests/workspace/profile-store.test.ts tests/app/app-hidden-items.test.tsx
git commit -m "实现隐藏路径持久化与显示隐藏项会话态"
```

---

### Task 2: 抽出隐藏可见性判断，并分离原始树与渲染树

**Files:**
- Modify: `src/workspace/file-tree-types.ts`
- Modify: `src/workspace/file-tree.ts`
- Modify: `tests/workspace/file-tree.test.ts`

- [ ] **Step 1: 写失败测试，锁定父隐藏优先级与原始树保留**

```ts
it('marks descendants as hidden when an ancestor path is explicitly hidden', () => {
  const tree = buildFileTree(['docs/guide.md', 'docs/private/secret.md'])
  const visibleTree = createVisibleFileTree({
    sourceNodes: tree,
    hiddenPaths: ['docs/private'],
    showHiddenItems: false,
  })

  expect(visibleTree).toEqual([
    expect.objectContaining({
      path: 'docs',
      children: [
        expect.objectContaining({ path: 'docs/guide.md' }),
      ],
    }),
  ])
})

it('keeps hidden descendants in the rendered tree when showHiddenItems is true', () => {
  const tree = buildFileTree(['docs/guide.md', 'docs/private/secret.md'])
  const visibleTree = createVisibleFileTree({
    sourceNodes: tree,
    hiddenPaths: ['docs/private'],
    showHiddenItems: true,
  })

  expect(visibleTree).toEqual([
    expect.objectContaining({
      path: 'docs',
      children: [
        expect.objectContaining({ path: 'docs/guide.md' }),
        expect.objectContaining({
          path: 'docs/private',
          meta: expect.objectContaining({ isExplicitlyHidden: true }),
          children: [
            expect.objectContaining({
              path: 'docs/private/secret.md',
              meta: expect.objectContaining({ isHiddenByAncestor: true }),
            }),
          ],
        }),
      ],
    }),
  ])
})

it('does not mutate or prune the source tree when deriving a visible tree', () => {
  const tree = buildFileTree(['docs/private/secret.md'])
  createVisibleFileTree({
    sourceNodes: tree,
    hiddenPaths: ['docs/private'],
    showHiddenItems: false,
  })

  expect(tree).toEqual([
    expect.objectContaining({
      path: 'docs',
      children: [
        expect.objectContaining({
          path: 'docs/private',
          children: [expect.objectContaining({ path: 'docs/private/secret.md' })],
        }),
      ],
    }),
  ])
})
```

- [ ] **Step 2: 运行 `file-tree` 测试，确认失败**

Run: `npm test -- tests/workspace/file-tree.test.ts`  
Expected: FAIL，缺少隐藏可见性辅助与元数据。

- [ ] **Step 3: 为节点增加渲染元数据类型**

```ts
export interface FileTreeNodeMeta {
  isExplicitlyHidden: boolean
  isHiddenByAncestor: boolean
  isVisibleInCurrentMode: boolean
}

export interface VisibleFileTreeFileNode extends FileTreeFileNode {
  meta: FileTreeNodeMeta
}

export interface VisibleFileTreeDirectoryNode extends FileTreeDirectoryNode {
  meta: FileTreeNodeMeta
  children: VisibleFileTreeNode[]
}

export type VisibleFileTreeNode = VisibleFileTreeFileNode | VisibleFileTreeDirectoryNode
```

```ts
export interface VisibleFileTreeResult {
  visibleNodes: VisibleFileTreeNode[]
  availableDirectoryPaths: string[]
}
```

- [ ] **Step 4: 实现隐藏判断和渲染树派生**

```ts
function listAncestorPaths(path: string): string[] {
  const segments = path.split('/').filter(Boolean)
  return segments.slice(0, -1).map((_, index) => segments.slice(0, index + 1).join('/'))
}

export function isPathExplicitlyHidden(path: string, hiddenPaths: Set<string>): boolean {
  return hiddenPaths.has(path)
}

export function isPathHiddenByAncestor(path: string, hiddenPaths: Set<string>): boolean {
  return listAncestorPaths(path).some((ancestorPath) => hiddenPaths.has(ancestorPath))
}

export function createVisibleFileTree({
  sourceNodes,
  hiddenPaths,
  showHiddenItems,
}: {
  sourceNodes: FileTreeNode[]
  hiddenPaths: string[]
  showHiddenItems: boolean
}): VisibleFileTreeResult {
  const hiddenPathSet = new Set(hiddenPaths)
  const availableDirectoryPaths = new Set<string>()

  function visit(node: FileTreeNode): VisibleFileTreeNode | null {
    const isExplicitlyHidden = isPathExplicitlyHidden(node.path, hiddenPathSet)
    const isHiddenByAncestor = isPathHiddenByAncestor(node.path, hiddenPathSet)
    const isHidden = isExplicitlyHidden || isHiddenByAncestor
    const isVisibleInCurrentMode = showHiddenItems || !isHidden

    if (node.kind === 'file') {
      if (!isVisibleInCurrentMode) {
        return null
      }

      return {
        ...node,
        meta: {
          isExplicitlyHidden,
          isHiddenByAncestor,
          isVisibleInCurrentMode,
        },
      }
    }

    const nextChildren = node.children
      .map(visit)
      .filter((child): child is VisibleFileTreeNode => child != null)

    availableDirectoryPaths.add(node.path)

    if (!isVisibleInCurrentMode && nextChildren.length === 0) {
      return null
    }

    return {
      ...node,
      children: nextChildren,
      meta: {
        isExplicitlyHidden,
        isHiddenByAncestor,
        isVisibleInCurrentMode,
      },
    }
  }

  const visibleNodes = sourceNodes
    .map(visit)
    .filter((node): node is VisibleFileTreeNode => node != null)

  return {
    visibleNodes,
    availableDirectoryPaths: [...availableDirectoryPaths],
  }
}
```

- [ ] **Step 5: 运行 `file-tree` 测试，确认通过**

Run: `npm test -- tests/workspace/file-tree.test.ts`  
Expected: PASS

- [ ] **Step 6: Commit**

```bash
git add src/workspace/file-tree.ts src/workspace/file-tree-types.ts tests/workspace/file-tree.test.ts
git commit -m "实现文件树隐藏可见性判断"
```

---

### Task 3: 在 App 中接入 `hiddenPaths`、`showHiddenItems` 和原始树/渲染树

**Files:**
- Modify: `src/App.tsx`
- Modify: `src/app/AppShell.tsx`
- Modify: `tests/app/app-shell.test.tsx`

- [ ] **Step 1: 写失败测试，锁定 props 透传与初始显隐状态**

```tsx
it('passes showHiddenItems and visible file tree data through AppShell to the workspace layout surfaces', () => {
  render(
    <AppShell
      projects={[]}
      activeProjectId={null}
      profileIds={['default']}
      activeProfileId="default"
      tabs={[]}
      activeTabId={null}
      mode="regular"
      regularViewState="locked"
      fileTree={[]}
      availableDirectoryPaths={[]}
      showHiddenItems={true}
      currentDocumentPath={null}
      currentDocumentContent={null}
      statusMessage="等待载入项目"
      sidebarWidth={280}
      outlineWidth={320}
      onConnectProject={() => {}}
      onProjectChange={() => {}}
      onProfileChange={() => {}}
      onModeChange={() => {}}
      onToggleRegularLock={() => {}}
      onToggleShowHiddenItems={() => {}}
      onHidePath={() => {}}
      onUnhidePath={() => {}}
      onTabSelect={() => {}}
      onTabClose={() => {}}
      onTabReorder={() => {}}
      onDocumentSelect={() => {}}
      onSidebarWidthChange={() => {}}
      onSidebarWidthCommit={() => {}}
      onOutlineWidthChange={() => {}}
      onOutlineWidthCommit={() => {}}
    />,
  )

  expect(screen.getByRole('button', { name: '显示隐藏项' })).toHaveAttribute('aria-pressed', 'true')
})
```

- [ ] **Step 2: 运行 AppShell 相关测试，确认失败**

Run: `npm test -- tests/app/app-shell.test.tsx`  
Expected: FAIL，缺少新 props 与按钮透传。

- [ ] **Step 3: 在 App 中引入 `sourceFileTree` 与 `showHiddenItems` 状态**

```ts
const [sourceFileTree, setSourceFileTree] = useState<FileTreeNode[]>([])
const [hiddenPaths, setHiddenPaths] = useState<string[]>([])
const [showHiddenItems, setShowHiddenItems] = useState(false)

const { visibleNodes: visibleFileTree, availableDirectoryPaths } = createVisibleFileTree({
  sourceNodes: sourceFileTree,
  hiddenPaths,
  showHiddenItems,
})
```

- [ ] **Step 4: 在 profile 恢复流程中接入 `hiddenPaths`，并在项目/profile 切换时重置 `showHiddenItems`**

```ts
setExpandedFileNodes(profile.navigation?.expandedFileNodes ?? [])
setHiddenPaths(profile.navigation?.hiddenPaths ?? [])
```

```ts
setShowHiddenItems(false)
```

- [ ] **Step 5: 在 `AppShell` 透传隐藏相关 props**

```tsx
<AppShell
  // ...
  fileTree={visibleFileTree}
  availableDirectoryPaths={availableDirectoryPaths}
  showHiddenItems={showHiddenItems}
  onToggleShowHiddenItems={handleToggleShowHiddenItems}
  onHidePath={handleHidePath}
  onUnhidePath={handleUnhidePath}
/>
```

- [ ] **Step 6: 实现最小的隐藏/解除隐藏保存逻辑**

```ts
async function handleHidePath(path: string) {
  const nextHiddenPaths = Array.from(new Set([...hiddenPaths, path]))
  setHiddenPaths(nextHiddenPaths)
  await saveActiveProfileNavigation({ hiddenPaths: nextHiddenPaths })
}

async function handleUnhidePath(path: string) {
  const nextHiddenPaths = hiddenPaths.filter((item) => item !== path)
  setHiddenPaths(nextHiddenPaths)
  await saveActiveProfileNavigation({ hiddenPaths: nextHiddenPaths })
}

async function handleToggleShowHiddenItems() {
  setShowHiddenItems((current) => !current)
}
```

- [ ] **Step 7: 运行 AppShell 相关测试，确认通过**

Run: `npm test -- tests/app/app-shell.test.tsx`  
Expected: PASS

- [ ] **Step 8: Commit**

```bash
git add src/App.tsx src/app/AppShell.tsx tests/app/app-shell.test.tsx
git commit -m "接入隐藏状态与文件树可见性链路"
```

---

### Task 4: 在 TopBar 增加“显示隐藏项”按钮，并保证 `split` 模式常驻

**Files:**
- Modify: `src/app/TopBar.tsx`
- Create or Modify: `tests/app/topbar-hidden-items.test.tsx`
- Modify: `src/index.css`

- [ ] **Step 1: 写失败测试，锁定 `regular` / `split` 模式按钮可见性**

```tsx
it('shows the hidden-items toggle in regular mode next to the lock button', () => {
  renderTopBar({ mode: 'regular', showHiddenItems: false })

  expect(screen.getByRole('button', { name: '显示隐藏项' })).toBeInTheDocument()
})

it('keeps the hidden-items toggle visible in split mode', () => {
  renderTopBar({ mode: 'split', showHiddenItems: true })

  expect(screen.getByRole('button', { name: '显示隐藏项' })).toBeInTheDocument()
  expect(screen.getByRole('button', { name: '显示隐藏项' })).toHaveAttribute('aria-pressed', 'true')
})
```

- [ ] **Step 2: 运行顶栏测试，确认失败**

Run: `npm test -- tests/app/topbar-hidden-items.test.tsx`  
Expected: FAIL，缺少 `showHiddenItems` props 与按钮渲染。

- [ ] **Step 3: 扩展 `TopBarProps` 并接入按钮**

```tsx
interface TopBarProps {
  // ...
  showHiddenItems: boolean
  onToggleShowHiddenItems: () => void
}
```

```tsx
<button
  type="button"
  className="topbar__lock-toggle topbar__hidden-toggle"
  aria-pressed={showHiddenItems}
  aria-label="显示隐藏项"
  title="显示隐藏项"
  onClick={onToggleShowHiddenItems}
>
  <span className="topbar__lock-icon" aria-hidden="true">
    {showHiddenItems ? <Eye /> : <EyeOff />}
  </span>
</button>
```

- [ ] **Step 4: 固化 `regular` / `split` 模式布局规则**

```tsx
<div className="topbar__mode-cluster">
  {isRegularMode ? (
    <button /* lock toggle */ />
  ) : null}

  <button
    type="button"
    className="topbar__lock-toggle topbar__hidden-toggle"
    aria-pressed={showHiddenItems}
    aria-label="显示隐藏项"
    title="显示隐藏项"
    onClick={onToggleShowHiddenItems}
  >
    <span className="topbar__lock-icon" aria-hidden="true">
      {showHiddenItems ? <Eye /> : <EyeOff />}
    </span>
  </button>

  <div className="topbar__modes" role="group" aria-label="模式切换">
    {/* existing mode buttons */}
  </div>
</div>
```

- [ ] **Step 5: 为按钮增加 pressed 样式**

```css
.topbar__hidden-toggle[aria-pressed='true'] {
  border-color: rgba(127, 105, 82, 0.32);
  background: linear-gradient(135deg, #f5c978 0%, #e8b154 100%);
  color: #4e3512;
}

.topbar__hidden-toggle[aria-pressed='false'] {
  background: rgba(255, 255, 255, 0.7);
  color: var(--text-strong);
}
```

- [ ] **Step 6: 运行顶栏测试，确认通过**

Run: `npm test -- tests/app/topbar-hidden-items.test.tsx`  
Expected: PASS

- [ ] **Step 7: Commit**

```bash
git add src/app/TopBar.tsx src/index.css tests/app/topbar-hidden-items.test.tsx
git commit -m "实现显示隐藏项顶栏按钮"
```

---

### Task 5: 在 WorkspaceLayout 接入节点级隐藏/解除隐藏交互

**Files:**
- Modify: `src/app/WorkspaceLayout.tsx`
- Modify: `tests/app/workspace-layout.test.tsx`
- Modify: `src/index.css`

- [ ] **Step 1: 写失败测试，锁定 hover 按钮语义与目录级联**

```tsx
it('hides a document when the file action button is clicked', async () => {
  const user = userEvent.setup()
  const onHidePath = vi.fn()

  render(
    <WorkspaceLayout
      mode="regular"
      regularViewState="locked"
      fileTree={createVisibleFileTree({
        sourceNodes: buildFileTree(['docs/guide.md']),
        hiddenPaths: [],
        showHiddenItems: false,
      }).visibleNodes}
      availableDirectoryPaths={['docs']}
      showHiddenItems={false}
      currentDocumentPath="docs/guide.md"
      currentDocumentContent={'# 标题'}
      statusMessage="当前项目：Notes"
      sidebarWidth={280}
      outlineWidth={320}
      hasProjects
      onDocumentSelect={() => {}}
      onHidePath={onHidePath}
      onUnhidePath={() => {}}
      onSidebarWidthChange={() => {}}
      onSidebarWidthCommit={() => {}}
      onOutlineWidthChange={() => {}}
      onOutlineWidthCommit={() => {}}
    />,
  )

  await user.click(screen.getByRole('button', { name: '隐藏 guide.md' }))
  expect(onHidePath).toHaveBeenCalledWith('docs/guide.md')
})

it('switches the action button to unhide when hidden items are visible', async () => {
  const user = userEvent.setup()
  const onUnhidePath = vi.fn()

  render(
    <WorkspaceLayout
      mode="regular"
      regularViewState="locked"
      fileTree={createVisibleFileTree({
        sourceNodes: buildFileTree(['docs/private/secret.md']),
        hiddenPaths: ['docs/private'],
        showHiddenItems: true,
      }).visibleNodes}
      availableDirectoryPaths={['docs', 'docs/private']}
      showHiddenItems={true}
      currentDocumentPath={null}
      currentDocumentContent={null}
      statusMessage="当前项目：Notes"
      sidebarWidth={280}
      outlineWidth={320}
      hasProjects
      onDocumentSelect={() => {}}
      onHidePath={() => {}}
      onUnhidePath={onUnhidePath}
      onSidebarWidthChange={() => {}}
      onSidebarWidthCommit={() => {}}
      onOutlineWidthChange={() => {}}
      onOutlineWidthCommit={() => {}}
    />,
  )

  await user.click(screen.getByRole('button', { name: '显示 private' }))
  expect(onUnhidePath).toHaveBeenCalledWith('docs/private')
})
```

- [ ] **Step 2: 运行工作区测试，确认失败**

Run: `npm test -- tests/app/workspace-layout.test.tsx`  
Expected: FAIL，缺少隐藏相关 props、按钮和样式语义。

- [ ] **Step 3: 扩展 `WorkspaceLayoutProps` 并为节点增加主体区与操作区**

```tsx
interface WorkspaceLayoutProps {
  // ...
  fileTree: VisibleFileTreeNode[]
  availableDirectoryPaths: string[]
  showHiddenItems: boolean
  onHidePath: (path: string) => void
  onUnhidePath: (path: string) => void
}
```

```tsx
<div className="file-tree__row" data-hidden={node.meta.isExplicitlyHidden || node.meta.isHiddenByAncestor ? 'true' : undefined}>
  <button
    type="button"
    className="file-tree__directory"
    aria-expanded={isExpanded}
    onClick={() => onToggleDirectory(node.path)}
  >
    <span className="file-tree__chevron" aria-hidden="true" />
    <span className="file-tree__directory-name">{node.name}</span>
  </button>

  <button
    type="button"
    className="file-tree__action"
    aria-label={node.meta.isExplicitlyHidden && showHiddenItems ? `显示 ${node.name}` : `隐藏 ${node.name}`}
    onClick={(event) => {
      event.stopPropagation()
      if (node.meta.isExplicitlyHidden && showHiddenItems) {
        onUnhidePath(node.path)
        return
      }
      onHidePath(node.path)
    }}
  >
    {node.meta.isExplicitlyHidden && showHiddenItems ? <Eye /> : <EyeOff />}
  </button>
</div>
```

```tsx
{showHiddenItems && node.meta.isHiddenByAncestor && !node.meta.isExplicitlyHidden ? (
  <span className="file-tree__derived-hidden-indicator" aria-hidden="true" />
) : null}
```

```tsx
const canUnhideDirectly = node.meta.isExplicitlyHidden && showHiddenItems
const shouldShowActionButton = !node.meta.isHiddenByAncestor || node.meta.isExplicitlyHidden
```

```tsx
{shouldShowActionButton ? (
  <button
    type="button"
    className="file-tree__action"
    aria-label={canUnhideDirectly ? `显示 ${node.name}` : `隐藏 ${node.name}`}
    onClick={(event) => {
      event.stopPropagation()
      if (canUnhideDirectly) {
        onUnhidePath(node.path)
        return
      }
      onHidePath(node.path)
    }}
  >
    {canUnhideDirectly ? <Eye /> : <EyeOff />}
  </button>
) : null}
```

- [ ] **Step 4: 为隐藏节点与操作区增加样式**

```css
.file-tree__row {
  display: flex;
  align-items: center;
  gap: 6px;
}

.file-tree__row[data-hidden='true'] .file-tree__directory,
.file-tree__row[data-hidden='true'] .file-tree__file {
  opacity: 0.52;
}

.file-tree__action {
  opacity: 0;
  pointer-events: none;
}

.file-tree__row:hover .file-tree__action,
.file-tree__row:focus-within .file-tree__action {
  opacity: 1;
  pointer-events: auto;
}
```

- [ ] **Step 5: 运行工作区测试，确认通过**

Run: `npm test -- tests/app/workspace-layout.test.tsx`  
Expected: PASS

- [ ] **Step 6: Commit**

```bash
git add src/app/WorkspaceLayout.tsx src/index.css tests/app/workspace-layout.test.tsx
git commit -m "实现文件树节点隐藏与解除隐藏交互"
```

---

### Task 6: 补齐搜索、展开状态和刷新恢复回归

**Files:**
- Modify: `tests/workspace/file-tree.test.ts`
- Modify: `tests/app/workspace-layout.test.tsx`
- Modify: `tests/app/app-shell.test.tsx`
- Optional modify: `tests/app/app-session-restore.test.tsx`

- [ ] **Step 1: 写失败测试，锁定隐藏目录后再次显示不丢展开状态**

```tsx
it('preserves expanded directories when hidden nodes are shown again', async () => {
  const user = userEvent.setup()
  const onExpandedDirectoriesChange = vi.fn()

  const { rerender } = renderVisibleWorkspace({
    hiddenPaths: ['docs/private'],
    showHiddenItems: false,
    persistedExpandedDirectories: ['docs', 'docs/private'],
    hasPersistedExpandedDirectories: true,
  })

  rerender(
    buildVisibleWorkspace({
      hiddenPaths: ['docs/private'],
      showHiddenItems: true,
      persistedExpandedDirectories: ['docs', 'docs/private'],
      hasPersistedExpandedDirectories: true,
      onExpandedDirectoriesChange,
    }),
  )

  expect(screen.getByRole('button', { name: 'private' })).toHaveAttribute('aria-expanded', 'true')
})
```

- [ ] **Step 2: 写失败测试，锁定刷新后 `showHiddenItems` 回到默认值**

```ts
it('starts with hidden items hidden again after a fresh mount', async () => {
  render(<App />)

  await waitFor(() => {
    expect(screen.getByRole('button', { name: '显示隐藏项' })).toHaveAttribute('aria-pressed', 'false')
  })
})
```

- [ ] **Step 3: 运行回归测试，确认失败**

Run: `npm test -- tests/workspace/file-tree.test.ts tests/app/workspace-layout.test.tsx tests/app/app-shell.test.tsx`  
Expected: FAIL，当前实现尚未完整覆盖这些约束。

- [ ] **Step 4: 修正实现细节并跑定向回归**

Run: `npm test -- tests/workspace/profile-store.test.ts tests/workspace/file-tree.test.ts tests/app/app-hidden-items.test.tsx tests/app/topbar-hidden-items.test.tsx tests/app/workspace-layout.test.tsx tests/app/app-shell.test.tsx`  
Expected: PASS

- [ ] **Step 5: 跑相关工作区总回归**

Run: `npm test -- tests/app/app-session-restore.test.tsx tests/app/app-profiles.test.tsx tests/app/topbar-reading-preferences.test.tsx tests/app/workspace-layout.test.tsx`  
Expected: PASS

- [ ] **Step 6: 构建验证**

Run: `npm run build`  
Expected: PASS

- [ ] **Step 7: Commit**

```bash
git add tests/workspace/file-tree.test.ts tests/app/workspace-layout.test.tsx tests/app/app-shell.test.tsx tests/app/app-session-restore.test.tsx
git commit -m "验证隐藏功能与工作区恢复链路"
```

---

## 自检清单

- [ ] `hiddenPaths` 只保存在 profile，不混入 local-state
- [ ] `showHiddenItems` 只保存在 `App` 的会话内 React state，不写回 profile 或 local-state store
- [ ] 原始树始终完整保留，渲染树只作为视图输入
- [ ] 隐藏目录后再次显示隐藏项时，已保存展开状态不会丢失
- [ ] `split` 模式下“显示隐藏项”按钮仍然可见
- [ ] 已隐藏节点在隐藏项可见模式下会切换为“显示 / 解除隐藏”按钮
- [ ] 隐藏当前文档不会自动关闭标签页或正文区域
- [ ] 搜索结果与当前显隐模式语义一致
- [ ] `npm run build` 和相关 Vitest 回归通过

## 执行交接

计划已完成并建议保存为 [2026-06-20-md-reader-文件夹与文档隐藏显示实施计划.md](C:/Code/md-reader/docs/方案/2026-06-20-md-reader-文件夹与文档隐藏显示实施计划.md)。

两种执行方式：

1. Subagent-Driven（推荐）
   - 我按任务逐个派子代理执行，并在任务间做 review

2. Inline Execution
   - 我在当前会话按这个实施计划直接开始改代码
